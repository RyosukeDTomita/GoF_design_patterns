// import type { Visitor } from './Visitor.js';

/**
 * Visitor(訪問者)を受け入れるインタフェース
 * NOTE: Javaのサンプルにはあったが、node.jsでは使用していない。
 * なぜなら、抽象クラスを継承する場合には必ず全ての抽象メソッドを実装する必要があるため、本クラスをあえてImplementしてEntryクラスを作る意味が薄いためである。
 */
// export interface Element {
//   accept(visitor: Visitor): void;
// }
